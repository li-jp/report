CREATE OR REPLACE FUNCTION ZLYJTC(ZLYJ IN NUMBER) RETURN NUMBER IS
  RESULT NUMBER;
BEGIN
  IF ZLYJ > 2000
  THEN
    RESULT := ZLYJ * 0.3;
  ELSIF ZLYJ > 0
  THEN
    RESULT := ZLYJ * 0.25;
  ELSE
    RESULT := 0;
  END IF;
  RETURN(RESULT);
END ZLYJTC;
