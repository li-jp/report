CREATE OR REPLACE FUNCTION DLYJTC(DLYJ IN NUMBER, LV IN VARCHAR2)
  RETURN NUMBER IS
  RESULT NUMBER;
BEGIN
  IF LV = '见习'
  THEN
    RESULT := DLYJ * 0.10;
  ELSE
    RESULT := DLYJ * 0.3;
  END IF;
  RETURN(RESULT);
END DLYJTC;
