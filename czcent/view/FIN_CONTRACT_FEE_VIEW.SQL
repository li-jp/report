create or replace view fin_contract_fee_view as
select t.bank_card,
       t.confirm_date,
       t.confirm_user_id,
       t.fee_code,
       t.fee_name,
       t.fee_type,
       t.id,
       t.input_date,
       t.input_user_id,
       t.is_rec,
       t.pay_date,
       t.pay_user,
       t.pay_way,
       t.contract_type,
       t.price,
       t.remarks,
       t.review_date,
       t.review_user_id,
       t.status,
       t.freezed,
       t.contract_id,
       t.receipt_number,
       t.customer_name
     from biz_contract_fee t;
